# SpacePilot AI

SpacePilot AI is a privacy-first Flutter app for understanding device storage,
finding unnecessary files, and keeping devices healthy with guided AI scans.

## Development

```powershell
flutter pub get
flutter run
```

Run checks with:

```powershell
flutter analyze
flutter test
.\gradlew.bat :app:assembleDebug
```
